import React from "react";

const Tasks = () => {
    return (
        <div className="page-container">
            <h2>Tasks</h2>
            <p>Track and manage tasks assigned to projects.</p>
        </div>
    );
};

export default Tasks;
